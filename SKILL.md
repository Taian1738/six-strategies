---
name: ltskill
description: >
  六韬：面向任意软件类型，以紧凑项目记忆和 SOL/Luna 分层执行低 token 多功能构建与长期维护；对新项目骨架及模块系统、跨模块公共契约、生命周期/资源所有权、持久化/迁移、IPC/事件总线、权限底座、插件系统、运行时/构建发布底座等底层架构提供 SOL Codex ultra 编排架构门、Luna max 有界实施、SOL max 不可分关键实施和统一全维收敛审计；初次创建含 GUI 的项目或用户明确指定时直接调用 `$wanxiang`，普通修复/重构默认不调用。可用 `$ltskill` 显式调用；除显式调用外，不自动用于普通单功能开发、纯解释或只读审查。
---

# 六韬

协议版本：`LT-2026-08-21.11`

以最少的上下文和新增代码、尽可能短的关键路径交付可验证的软件，并留下足够精确的功能索引，使后续修改只需读取相关文件。

架构、编码、修复和代码审查应用 Ponytail `full`：先复用现有实现，再选标准库、平台原生能力和已安装依赖，最后才写最少的新代码。不得因此省略用户明确要求的功能、信任边界校验、防止数据丢失的错误处理、安全措施、无障碍基础或必要验证。纯事实日志/索引只执行单一事实来源和最短充分记录，不加载 Ponytail；Ponytail 不可用时，编码类工作直接执行上述次序。

## 运行完整性与入口速查

启动时用本地文件/文本工具机械检查以下六个 reference 存在、可读，文件头 `适用协议` 与本文件一致，所有必需章节标题存在，且最后一行完整性标记匹配；机械扫描不等于把正文加载进主控上下文，之后只读取当前路线需要的文件：[dispatch-contracts.md](references/dispatch-contracts.md)、[sol-cost-controls.md](references/sol-cost-controls.md)、[audit-routing.md](references/audit-routing.md)、[foundation-gate.md](references/foundation-gate.md)、[project-memory.md](references/project-memory.md)、[change-log.md](references/change-log.md)。任一缺失、不可读、版本/章节/末尾标记不一致时返回 `BLOCKED_PROTOCOL`，列出精确文件、缺项和期望/实际值，不得静默省略对应门禁。

```text
dispatch-contracts: [## 通用任务包] [## Luna 事实取证包] [## SOL 架构包] [## Luna 实现包] [## SOL 审计修复契约] [### 分模型滑动并发池] [## SOL 审计包] [## Luna 日志包] | <!-- LT-COMPLETE:LT-2026-08-21.11:dispatch-contracts -->
sol-cost-controls: [## 一、模型与思考强度] [## 二、最小充分上下文] [## 三、MCP 与外部证据] [## 四、输出与复用] [## 五、项目记忆与缓存] [## 六、不得为节省消耗而做的事] | <!-- LT-COMPLETE:LT-2026-08-21.11:sol-cost-controls -->
audit-routing: [## 审计次数] [## 触发与跳过] [## 冻结输入] [## 普通单次全维审计] [## 专项工具门] [## 输出、修复与复审] | <!-- LT-COMPLETE:LT-2026-08-21.11:audit-routing -->
foundation-gate: [## 触发边界] [## 固定角色和阶段] [## 双门流程] [## 应用架构最小闭包] [### `AC-7` 适用性] [## 强制统一审计矩阵] [## 阶段化证据与防假通过] [## 确定性门与 PASS] [## 收敛与 BLOCKED] [## 审计输出] | <!-- LT-COMPLETE:LT-2026-08-21.11:foundation-gate -->
project-memory: [## 复用优先] [## 默认文件布局] [## `必读.md` 模板] [## 功能文档模板] [## 更新规则] | <!-- LT-COMPLETE:LT-2026-08-21.11:project-memory -->
change-log: [## 何时必须写] [## 文件与索引] [## Luna 写入流程] [## 日志模板] [## 后续读取] | <!-- LT-COMPLETE:LT-2026-08-21.11:change-log -->
```

| 入口 | 直接路线 |
|---|---|
| 新项目或多功能构建 | 先执行“GUI 与万相调用门”，再执行步骤 1–3；命中底层触发时转 `foundation-gate`，否则走普通批次。 |
| 功能修改、修复或优化 | 先执行“GUI 与万相调用门”，再执行步骤 1–2；若命中底层触发或属于跨模块高风险，先走步骤 3，再走步骤 4–8，否则直接走步骤 4–8。 |
| 底层架构建立或重构 | 先执行“GUI 与万相调用门”，再执行“底层架构触发”和 `foundation-gate` 双门，不套普通次数。 |

普通统一审计的五项核心是：缺陷与正确性、功能完整性、验证可信度、性能与资源、架构与集成；底层任务改用 `foundation-gate` 的 14 项矩阵。建立或重构可运行应用、服务、CLI、插件宿主或发布运行时时，还必须冻结并审计 `AC-0` 至 `AC-8` 应用架构最小闭包。

## 默认主模型

启动软件构建任务时，若需由用户或主控选择本任务的主模型，默认选择 **`gpt-5.6-sol` + `high`**。复杂新系统、跨多个核心域、公共 API、认证/权限、支付、隐私、迁移、并发、数据安全或发布基础设施任务改用 **`gpt-5.6-sol` + `xhigh`**。主模型负责澄清、路由、整合和风险判断；通过 Luna 能力门的有界实现交给 Luna，未通过且无法继续冻结的关键实现交给独立实现 SOL。普通任务只有命中 [references/audit-routing.md](references/audit-routing.md) 的正式审计触发项时，才在最终变更集冻结后由独立 SOL 运行一次终审并按现有契约统一复审一次；该文件的跳过规则优先，底层架构按 [references/foundation-gate.md](references/foundation-gate.md) 执行例外门禁。

主控的单代理 effort 只使用 `high` 或 `xhigh`。普通关键架构仅在能明确拆成至少两个真正独立的工作流且并行探索收益高于重复上下文成本时，才让架构子任务使用 Codex `ultra`；底层架构按本协议固定使用 SOL Codex `ultra`，不以普通并行条件覆盖。不得自动选择或回退到 Terra；只有用户明确指定 Terra 时才使用。

## 选择工作模式

- **新项目或多功能构建**：澄清可验收目标，建立最小架构，按独立功能批次实现并建立项目记忆。
- **功能修改、修复或优化**：优先从 `必读.md` 或仓库已有的等价索引定位功能与文件，再按受影响功能、路径或公共契约检索重大改动日志；没有索引时，用入口、符号和调用者搜索回退。只读取命中的日志、相关调用链、测试和接口，做最小根因修改。
- 用户给出反馈后，从受影响功能的影响分析继续；不要重新扫描整个项目或重做已通过阶段。

## GUI 与万相调用门

进入影响面判断时记录 `WANXIANG_ROUTE=ON|OFF|N/A` 和唯一理由：当前任务没有 GUI、前端、页面壳、组件系统、交互或视觉层工作时固定为 `N/A` 并停止万相路由；用户明确禁止万相时固定为 `OFF`；其余存在 GUI 工作的任务只能在以下任一条件成立时写 `ON`：

1. 当前任务是从无既有产品基线开始初次创建项目，且本次交付包含可运行 GUI、前端或 UI 系统。
2. 用户在当前任务中明确要求使用“万相”或 `$wanxiang` 创建、重构、换肤、改版或实施界面。

其余情况固定为 `OFF`：已有项目的修复、排错、重构、优化、迁移、框架升级、功能迭代或普通 UI 修改，即使改到界面文件、布局、组件或交互，也只复用现有设计系统并做六韬要求的最小根因改动；“重构界面”“优化 GUI”本身不等于明确要求万相。向已有后端首次增加界面仍属于已有项目，用户未明确指定时保持 `OFF`。每个新用户任务重新判定；初建任务内的 GUI 修正沿用该任务已冻结的 `ON`。

`ON` 时不得先另做一套重复 UI 方案，必须直接读取并执行当前 `$wanxiang`；若该 skill 缺失或不可读，返回 `BLOCKED_PROTOCOL`。万相负责 GUI 的宿主映射、风格契约、视觉系统、组件/状态、交互、输入、无障碍、平台行为和运行结果验证；六韬继续负责需求范围、底层/公共契约、数据/IPC/权限不变量、模型与文件所有权、Luna 能力门、统一审计和总体交付。万相是工作协议而非模型别名，不改变本文件的 SOL/Luna 路由；其产出的 GUI 契约必须先冻结进 `R-/A-/F-/T-` 和实现包，Luna 才可执行。万相验证证据并入六韬同一冻结变更集和统一审计，不按页面、组件或视觉维度另开 SOL 审计。

`OFF` 时不得读取、调用或让子代理隐式套用万相，也不得借“提升视觉质量”扩大成换肤、设计系统替换或无关布局重做；若现有视觉规范不足以完成用户明确行为，只在现有体系内补最小规则，除非用户随后明确启用万相。

## 底层架构触发

“底层架构”仅指以下任一底座的建立或重构：新项目骨架；模块系统；跨模块公共契约或公共接口底座；全局生命周期/资源所有权；持久化/迁移基础设施；IPC/事件总线底座；权限底座；插件系统；运行时、构建或发布底座。运行时/构建/发布底座仅在建立或重构共享构建图、依赖闭包机制、产物组合根、发布 gate、更新/回滚流水线或平台抽象时命中；单个依赖、版本、配置键或产物成员变化若不改变这些机制及契约，只走普通风险路由。普通 API 方法/端点、单个模块、局部生命周期、业务功能、局部 UI、单点修复、局部优化及不改变底座契约的迭代不触发底层架构门。事实不足以判定是否改变底座契约时，先运行 `LUNA_FACT_GATE` 有界取证再唯一判定，不得凭“涉及构建/发布”字样升级，也不得按低风险跳过。命中后必须读取并执行 [references/foundation-gate.md](references/foundation-gate.md)，架构门和实现门均需 PASS；不得用普通任务的审计次数或模型强度替代底层门。

同一交付同时含底层重构和普通功能时，凡普通功能依赖、改变或验证该底层契约，或共享文件、状态、迁移、集成验收，均并入同一个底层冻结变更集和实现门。只有同时给出以下四项可复核证据才分开走普通路由：公共契约/调用依赖交集为空；精确文件与符号写集不重叠；进程、端口、锁、数据库、迁移、缓存及其他状态资源不共享；两部分可分别执行验收且无需对方产物。任一项重叠、未知或只能口头声明时必须并入底层冻结集。

## 控制上下文

1. 每次软件任务启动时读取本文件；跨日续跑、长时间暂停、上下文压缩/恢复、任务移交或明确得知 skill 已更新后，在继续决策或派发前重读当前 `SKILL.md` 及本阶段引用的 reference，并以文件中的最新协议版本为准。版本变化只约束后续工作：停止发送旧格式新任务，待正在运行的写入者安全结束后再重排队列；不强行中断代理、不重做已通过工作。
2. 先读适用的 `AGENTS.md`、现有项目索引/架构文档、项目清单/配置和工作树状态。
3. 用文件搜索和符号搜索定位入口、调用者、测试及共享接口；跳过生成物、依赖目录和无关二进制。
4. 优先传递文件路径、符号名、必要片段、当前 diff 和测试结果；不要向代理复制完整仓库、完整对话或整份无关文档。
5. 修改时复用既有结构，不因“以后可能需要”增加层、接口、配置或依赖。
6. 可逆且低风险的缺失信息采用明确假设继续；会改变产品方向、数据或外部状态时再询问用户。
7. 保持运行环境的模型窗口和自动压缩默认值，不全局强开长上下文。只有索引、日志、按功能拆分和无损压缩仍无法容纳关键事实时，才为单次任务使用扩展窗口；启用前核实当前模型目录、计费阈值和实际输入规模。

## 分层使用模型

能由主控直接可靠完成的小任务不要委派。需要委派时读取 [references/dispatch-contracts.md](references/dispatch-contracts.md)。

| 协议代号 | 固定模型 ID | 定位 |
|---|---|---|
| SOL | `gpt-5.6-sol` | 旗舰能力：主控、未冻结决策、关键架构、不可分的关键实现与独立审计。 |
| Luna | `gpt-5.6-luna` | 成本优先：上下文有界、决定已冻结、验证精确的实现、取证与机械工作。 |

`none/low/medium/high/xhigh/max` 是单模型 `reasoning.effort`；提高 effort 增加同一模型的推理预算，不把 Luna 变成 SOL。`max` 是最高单代理 effort。`ultra` 在本协议中专指 Codex 的多智能体编排模式；Codex 运行时若把它暴露为代理创建参数，使用该原生字段，但不得在原始 Responses API 中发送 `reasoning.effort="ultra"`。原始 API 需要类 ultra 行为时使用所选模型的 `max` 加 Multi-agent beta，并保持本协议的独立工作流和统一汇总结论；检测到原始 API 的 `reasoning.effort="ultra"` 时返回 `BLOCKED_MODEL` 并拒绝调用，修正执行模式后重新预检。每个 `ultra` 是一个顶层编排任务，其内部工作者按实际模型计入对应滑动并发池并共同受环境全局限制；统一审计只允许一个顶层审计任务和一份总判定，内部工作者只能提供证据，不得各自形成独立审计、修复契约或追加复审轮次。

### Luna 事实取证门

严格只读、只抄录可定位事实的任务使用 `LUNA_FACT_GATE=PASS`，不套实现能力门的“决定已冻结”前提。派发前必须逐项给出可复核证据：

1. 读取范围限定为列明的路径、符号、字段、diff 和只读命令；不要求全仓搜索、扩展窗口或自行扩大范围。
2. 输出白名单仅含现存声明/配置/schema 原文定位、精确调用边、测试或工具原始结果，以及相互冲突或缺失的事实；不得输出架构结论、风险判定、修复方案或优先级。
3. 不要求 Luna 选择或推导产品行为、架构、API/schema、算法、错误语义、信任边界、权限策略或长程状态关系；一旦问题需要这些语义归纳，立即停止并把原始定位交给主控/SOL。
4. 允许修改固定为“无”，命令无写入、生成、安装、启动后台服务或真实外部状态副作用；预授权的只读外部查询仍须遵守任务包预算。
5. 每条事实携带 `path:symbol/line` 或命令及关键原始结果；无法确认时只返回 `UNKNOWN`，冲突时并列原始证据，不猜测、不投票、不宣布 PASS/FAIL。

`LUNA_FACT_GATE` 只用于事实取证，不能用于实现、返修、日志归纳、架构设计或审计，也不能把取证输出直接当作已冻结契约。主控或设计 SOL 必须核对证据并完成全部语义决定；之后任何写入仍须独立通过 `LUNA_GATE`。普通有界事实取证使用 Luna `low/medium`，底层架构可实施性取证固定 Luna `max`；effort 不扩大输出白名单。

### Luna 能力门

每个 Luna 实现、返修或跨模块日志包在派发前必须写 `LUNA_GATE=PASS`，并逐项给出可复核证据：

1. 所需事实限定在列明的路径、符号、diff 和冻结摘要内，不要求全仓归纳、扩展窗口或从遗漏上下文猜测。
2. 产品行为、架构、API/schema、算法和错误语义均已唯一冻结。
3. 信任边界、权限策略、敏感数据不变量均已冻结，不要求 Luna 推导或审查安全策略。
4. 状态机、并发/锁、生命周期、迁移、幂等、回滚和 cleanup 时序均已枚举；没有未决的长程状态关系。
5. 写集、运行资源、精确测试/失败注入/构建命令和断言完整，关键项无 `UNKNOWN`。

任一项为否或未知时禁止派发 Luna。先由 SOL `max` 解析并冻结缺口；若仍无法形成通过能力门的有界批次，由与设计者和审计者分离的实现 SOL `max` 直接完成最小不可分关键切片。该例外只处理“任务内容超过 Luna 能力门”，不是 Luna 模型或必需执行模式不可用时的替代回退；后者必须按可用性状态处理。审计 SOL 只给唯一修复契约，不得亲自修改产品文件。跨模块、信任边界或长程状态只是触发能力门，不自动把已经完全冻结、可独立验证的机械实现升级为 SOL。

- **普通 SOL 高价值推理**：仅用于普通新项目的关键架构、跨模块契约设计、未通过 Luna 能力门的最小不可分关键实现和整体审计；只有不可逆数据变更、真实环境切换或继续实现会放大重大损失时才允许一次边界明确的中途审计。默认使用 `fork_turns: "none"`、`model: "gpt-5.6-sol"`、单代理 `max`。审计必须读取 [references/audit-routing.md](references/audit-routing.md)，覆盖五项核心维度并只展开被变更触发的专项维度。安全维度仅在需求或变更直接涉及信任边界、外部输入、秘密、个人数据或法规约束时按实际影响低优先级展开；这不等于默认运行第三方安全插件或全域安全扫描。普通架构只有至少两个独立工作流时才使用 Codex `ultra`。
- **底层架构 SOL**：命中底层架构触发时，架构设计/冻结和独立架构契约审计固定使用 `gpt-5.6-sol` + Codex `ultra`；通过 Luna 能力门的有界搭建、返修和确定性门批次使用 `gpt-5.6-luna` + `max`，未通过且无法继续冻结的最小不可分关键切片使用独立实现 SOL `max`，严格遵守 [references/foundation-gate.md](references/foundation-gate.md)。每轮只有一个统一审计 SOL，不按维度、组件或文件拆分，且不得兼任实现者。
- **普通 Luna 分级实现**：仅在 `LUNA_GATE=PASS` 后分级。`low` 用于低复杂度、低后果、测试充分的机械实现、局部测试、索引和事实明确的日志；`medium` 用于有界的非平凡语义或测试补强；`high` 用于契约已冻结且可局部验证的公共 API/外部端点、认证接线、迁移步骤、并发实现、复杂算法及其他高后果批次，并接受独立 SOL 审计。均使用 `fork_turns: "none"`、`model: "gpt-5.6-luna"`。底层有界实现固定 Luna `max`；effort 不能覆盖能力门失败。
- **可用性状态**：派发前预检模型 ID、执行模式、effort、所需工具和审计独立性，并按唯一优先级判定：全部满足写 `AVAILABLE`；只有可选并行编排缺失且同一必需模型可用单代理 `max` 完成普通任务时写 `DEGRADED`；精确命中下述唯一自动吸收例外时写 `SOL_ABSORBED`；否则，若存在一个技术上可执行但会改变用户指定模型、成本、角色独立性或外部行为的具体替代，写 `NEEDS_USER` 并列出该替代；没有这种具体替代而必需 SOL/Luna、底层 `ultra`、工具或独立审计不可用时写 `BLOCKED_MODEL`。唯一自动吸收例外是：现有 SOL 主控可直接完成一个普通、已通过能力门的 Luna 有界批次，不新建替代调用。底层不允许模型替代，必需能力缺失直接 `BLOCKED_MODEL`；解除条件可以是恢复能力或用户另行改变任务契约，但不同时标记 `NEEDS_USER`。不得自动使用 Terra，也不得使用“最强”或“胜任”等未预检的定性回退。
- **分模型滑动并发池**：Luna 与 SOL 各自最多 **6 个同时活跃子代理**；分别取 `min(6, 该模型池可用槽位, 运行时剩余总槽位)`，且包括主控在内仍不得突破环境全局并发限制。这里的 6 是同一时刻的容量，不是一次性批次或整个任务的累计调用数；代理完成、返回当前回合或停止工作即释放对应模型槽位，主控可立即从该池的就绪队列补入新任务，因此生命周期内先后创建数可以超过 6。不得为凑数拆碎任务或让 SOL 空置。SOL 并行只用于真正独立的普通关键架构/推理工作流；普通正式终审及集中复审各始终只用一个 SOL，不按组件、文件或维度拆分。底层架构的每轮统一审计同样只用一个独立 SOL，按门禁规则重复而不拆分。
- SOL 参与前必须读取 [references/sol-cost-controls.md](references/sol-cost-controls.md)，先登记本交付生命周期的 SOL 回合预算。默认由主控或 Luna 收集外部证据，再把带来源的最小证据包交给 SOL；SOL 不自行漫游 MCP、网页或整个仓库。普通全维审计遵守一次终审加一次统一复审；底层架构按门禁重复统一全维审计直到 PASS 或 BLOCKED。专项工具结果并入对应审计，不启动按维度或组件拆分的自由式复审。
- 委派 SOL 必须即时创建、成批提问并尽快结束：不为将来阶段预开审计者，不让 SOL 在 Luna 实现期间空置。只有关键架构可真实拆成互不依赖的工作流时才并发多个 SOL；否则合并为一个任务。短间隔、同角色增量且预计总输入更小时才复用；长时间停顿、上下文已压缩或缓存状态未知时，先重读当前协议，再用项目索引/决策增量创建最小新任务包，不重放旧对话。
- 委派必须使用最小任务包。普通代理禁止自行继续嵌套委派；任务包明确写 `codex_ultra` 或 `responses_multi_agent_beta` 即视为只授权该顶层编排者创建本任务所需的内部工作者，内部工作者不得再委派，且全部计入实际模型池和环境全局槽位。除此以外只有主控另行明确授权才可委派。
- 写入任务只有在文件集合互不重叠、共享契约已冻结、验证可独立运行，且命令不争用 Git 索引、锁文件、生成目录、数据库、端口、后台进程或真实外部状态时才并行；否则使用隔离工作树/临时目录或改为串行。只读任务可在架构前并发，但应按不同问题或模块划分，避免多个 Luna 重复扫描同一区域。
- 委派前先按任务类型运行事实取证门或实现能力门，再按取证规模或实现的语义复杂度、后果风险和测试充分度选择强度，不以“先让 low 失败两次”作为升级条件。一个代理连续两次无法解决同一问题时停止重复消耗；事实取证越过输出白名单则停止并转主控/SOL，能力门仍可通过则由主控重新冻结一次任务包，能力门失败则按本节交独立实现 SOL 或返回 `BLOCKED_MODEL/NEEDS_USER`。

## 执行流程

### 1. 固定目标

把需求压缩成：目标、带 `R-` 编号的可观察验收条件、明确不做的内容、平台/技术约束和未知项。只记录会改变实现决策的信息。单一琐碎任务可省略编号。

### 2. 判断风险与影响面

定位入口、调用链、数据流、公共接口、已有测试和可能冲突的在途修改。两个以上互不依赖且工作量足以覆盖调度开销的模块或调查问题，只有分别满足 `LUNA_FACT_GATE=PASS` 后才通过 Luna 工作池并发只读定位；Luna 只返回白名单事实，由主控/SOL 汇总并作语义判断，不重复扫描。涉及认证、权限、支付、隐私、迁移、数据丢失、并发、公共 API、部署或多个核心模块时视为高风险。

### 3. 选择最小架构

新项目或跨模块高风险任务才输出架构契约：目标与非目标、模块边界、数据流、稳定接口、不变量、失败路径、安全边界、已知扩展缝、关键取舍、验证路径和风险。先按“底层架构触发”判定：命中时读取 [references/foundation-gate.md](references/foundation-gate.md)，由 SOL Codex `ultra` 设计并冻结架构和可执行批次，独立 SOL Codex `ultra` 统一审计架构契约；PASS 后，通过能力门的批次由 Luna `max` 搭建，未通过且不可继续冻结的最小不可分关键切片由独立实现 SOL `max` 完成。未命中时保留普通分级，主控已是 SOL `high/xhigh` 且架构不属于关键/高风险时直接完成，不另开 SOL，关键架构默认使用一个 SOL `max` 子任务，只有能明确拆成 2–6 个互不依赖的普通关键工作流时才使用 Codex `ultra`。用 `A-` 决策关联对应的 `R-` 需求，并要求 SOL 给每个批次写明能力门证据、执行者和可执行实现契约。任何新增模块、服务、公共接口、进程/通信桥、插件点、队列、缓存、配置或依赖都必须先通过 Ponytail 最小性准入；应用底层任务再逐项冻结 `AC-0` 至 `AC-8`，不得把特定框架的“万物皆插件”当作通用默认。优先成熟框架默认结构和真实需要的扩展点，不预建未使用抽象；不得用硬性字数上限截断必要设计。

### 4. 划分实现批次

按功能边界和文件所有权划分 `F-` 批次，并把需求映射为 `R- →（A-，有架构决定时）→ F- → 文件/符号 → T-验证`。每个 Luna 批次必须是可直接执行的实现包：写明唯一已决方案、正常/失败/恢复行为、逐文件/符号修改顺序、入口与调用链、复用点、接口签名/schema 前后值、算法/伪代码、状态/并发/错误/cleanup 时序、冻结不变量、精确允许/禁止写入集合、依赖顺序、测试用例/断言/命令、成功判据、回滚及冲突停止条件。精简只删除无关上下文，不得省略任何会迫使 Luna 选择产品行为、架构、接口、算法或错误语义的信息；任一关键字段缺失时不派发。建立依赖图和 Luna 就绪队列：共享契约、上下文和验证方式相同的琐碎工作合并；其余安全且独立的实质批次持续补槽，依赖未满足或共享状态未隔离的批次串行处理。

### 5. 实现并即时验证

每个 Luna 在其批次内运行最小但有效的独立检查：相关单元/集成测试、静态检查或可重复的手动验证；无共享状态的验证可并发，有 CPU、内存、I/O、端口、数据库或生成目录争用的验证限流或串行。修复根因并检查调用者，不以局部绕过掩盖共享缺陷。非平凡逻辑至少留下一个能捕获回归的可运行检查。共享清单、路由、注册表、迁移、公共类型和最终合并只交给主控或一个指定整合者；全部批次合并后再运行一次最小跨功能回归/构建检查，解决集成冲突后才冻结变更集。

### 6. 更新当前状态项目记忆

新项目、多功能项目或预期持续维护的项目，读取 [references/project-memory.md](references/project-memory.md)。在终审前更新仓库已有的等价当前状态索引；不存在时才创建精简的 `必读.md` 与确有必要的功能文档，并落盘会影响后续修改的 `A-` 架构决定。索引是本次冻结变更集的一部分。只修正会妨碍导航或维护的结构/命名不一致，不做纯审美重命名。

### 7. 统一全维审计与返修

读取并应用 [references/audit-routing.md](references/audit-routing.md)。实现期间只做自检、测试和主控 diff 检查；全部功能、当前状态索引和适用证据稳定后，命中正式审计触发项的普通任务由一个无历史独立 SOL 对冻结变更集终审，集中修复后统一复审一次，命中跳过项的任务不创建 SOL。底层架构必须先通过架构契约门，搭建后再由同一独立审计角色每轮覆盖完整底层矩阵；每次 FAIL 先输出唯一完整修复契约并标明能力门证据和执行者：架构门的机械文档修订可由通过能力门的 Luna `max` 执行，架构决定/契约修订由原设计 SOL Codex `ultra` 执行；普通任务和实现门中，通过门的有界批次由对应 Luna 执行，未通过且不可继续冻结的最小不可分关键切片由独立实现 SOL `max` 执行。之后重跑确定性门、重冻结和统一全维审计，直到 PASS 或明确 BLOCKED。普通和底层审计均不得按组件、文件或维度拆分；审计者不得兼任设计者或实现者；原生 `/review` 只有完整满足对应契约时才可直接采纳。

### 8. 写重大改动日志并交付

代码发生重大改动时必须读取并应用 [references/change-log.md](references/change-log.md)：适用终审通过后（无需终审的改动则在验证完成后），由 Luna 串行写一份可追溯的集成级日志并更新日志索引；主控核对事实、审计链接和覆盖关系。重大程度按接口、数据、行为和影响面判断，不按代码行数判断。日志是历史元数据，不为它另开 SOL 审计；琐碎修改和纯机械生成物不写日志。

确认验收条件、相关验证、项目记忆和剩余风险。向用户简洁报告结果与可执行的下一步。收到修改意见后仅重新打开受影响功能。

打包、发布、安装依赖、写入项目外目录或创建桌面快捷方式，仅在用户请求或当前授权明确涵盖时执行；破坏性操作及真实环境变更仍需相应确认。

## 完成标准

- 用户要求的行为可观察地成立。
- 相关测试或等效验证通过；无法运行的检查及原因已说明。
- 没有已知的阻断/高风险缺陷，跨功能契约保持一致。
- 多功能或高风险任务的每个 `R-` 都能追溯到可选架构决定、功能、实现位置和 `T-` 验证，且每项实际变更都对应某个 `R-` 或明确非目标内的必要工程工作；适用时已通过核心维度及所有触发专项维度的审计。
- 仓库现有等价主索引（默认布局下为 `必读.md`）和受影响功能索引已按需更新，没有复制源码或堆积无关历史。
- 若属于重大改动，Luna 已写入一份集成级日志并更新日志索引；后续可按功能、路径、公共契约或数据结构只读取相关日志。
- 若属于底层架构，架构门和实现门均已 PASS，且不存在 UNKNOWN、未关闭确认问题或不稳定任务指纹。
- 本次工作未引入无关改动，并完整保留用户已有改动；最终说明保持简短。

